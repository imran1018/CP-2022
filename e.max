#include <stdio.h>
int main()
{
    int i,n,x,max;
    i = 1;
    max = -1;
    
    scanf("%d", &n);
    while(i <= n)
    {
        scanf("%d", &x);
        
        if(x > max) max = x;
        i++;
    }
    printf("%d", max);
    return 0;
}
